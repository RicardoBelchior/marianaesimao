<!-- Intro Section -->
<%= render partial: 'home/intro' %>

<% if logged_in? %>
  <!-- Story Section -->
  <%#= render partial: 'home/story' %>

  <!-- Info Section -->
  <!-- <%= render partial: 'home/info' %> -->

  <!-- Map Church Section -->
  <%= render partial: 'home/map_church' %>

  <!-- Map Party Section -->
  <%= render partial: 'home/map_party' %>

  <!-- Suggestions Section -->
  <%= render partial: 'home/our_suggestions' %>

  <!-- Suggestions Section -->
  <%= render partial: 'home/suggestions' %>

  <!-- Contact Section -->
  <%= render partial: 'home/contact' %>

  <!-- List Section -->
  <!-- <%= render partial: 'home/list' %> -->

<% end %>
